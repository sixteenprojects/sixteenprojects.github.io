/** graph.js — D3 network graph stub (Phase 5) */
const GraphView = { render(container) {
  container.innerHTML = '<div class="loading-overlay"><p style="color:var(--text-muted)">Threat Graph — coming in Phase 5</p></div>';
}};
export default GraphView;
