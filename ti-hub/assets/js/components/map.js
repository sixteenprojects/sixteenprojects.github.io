/** map.js — Leaflet world map stub (Phase 5) */
const MapView = { render(container) {
  container.innerHTML = '<div class="loading-overlay"><p style="color:var(--text-muted)">World Map — coming in Phase 5</p></div>';
}};
export default MapView;
